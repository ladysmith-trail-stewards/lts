# RLS Policies

> Auto-generated by `scripts/extract-db-policies.js` on 2026-03-24T05:36:04.053Z.
> Re-run after `pnpm db:reset` or schema changes: `pnpm db:policies`.
>
> **For AI reference** — describes the exact RLS policies
> active in the local Supabase instance.

---

## Access Matrix

> ✅ = always &nbsp;·&nbsp; 📍 = own region only &nbsp;·&nbsp; 👤 = own record only &nbsp;·&nbsp; — = no access
>
> `service_role` bypasses RLS entirely and is excluded from this matrix.

### `profiles`

| Role | SELECT | INSERT | UPDATE | DELETE |
|---|:---:|:---:|:---:|:---:|
| Anon | — | — | — | — |
| User | 👤 | — | 👤 | — |
| Super User | 👤 | — | 👤 | — |
| Admin | 📍 | 📍 | 📍 | 📍 |
| Super Admin | ✅ | ✅ | ✅ | ✅ |

### `regions`

| Role | SELECT | INSERT | UPDATE | DELETE |
|---|:---:|:---:|:---:|:---:|
| Anon | ✅ | — | — | — |
| User | ✅ | — | — | — |
| Super User | ✅ | — | — | — |
| Admin | ✅ | — | — | — |
| Super Admin | ✅ | ✅ | ✅ | ✅ |

### `trails`

| Role | SELECT | INSERT | UPDATE | DELETE |
|---|:---:|:---:|:---:|:---:|
| Anon | — | — | — | — |
| User | ✅ | — | — | — |
| Super User | ✅ | 📍 | 📍 | 📍 |
| Admin | ✅ | 📍 | 📍 | 📍 |
| Super Admin | ✅ | ✅ | ✅ | ✅ |


---

## Policy Detail

### `profiles`

| Policy name | Command | USING | WITH CHECK |
|---|---|---|---|
| `profiles: admin delete local` | `DELETE` | `((get_my_role() = 'admin'::app_role) AND (region_id = get_my_region_id()))` | — |
| `profiles: admin insert local` | `INSERT` | — | `((get_my_role() = 'admin'::app_role) AND (region_id = get_my_region_id()))` |
| `profiles: admin select local` | `SELECT` | `((get_my_role() = 'admin'::app_role) AND (region_id = get_my_region_id()))` | — |
| `profiles: admin update local` | `UPDATE` | `((get_my_role() = 'admin'::app_role) AND (region_id = get_my_region_id()))` | `((get_my_role() = 'admin'::app_role) AND (region_id = get_my_region_id()))` |
| `profiles: select own` | `SELECT` | `(auth_user_id = auth.uid())` | — |
| `profiles: super_admin delete` | `DELETE` | `(get_my_role() = 'super_admin'::app_role)` | — |
| `profiles: super_admin insert` | `INSERT` | — | `(get_my_role() = 'super_admin'::app_role)` |
| `profiles: super_admin select all` | `SELECT` | `(get_my_role() = 'super_admin'::app_role)` | — |
| `profiles: super_admin update any` | `UPDATE` | `(get_my_role() = 'super_admin'::app_role)` | `(get_my_role() = 'super_admin'::app_role)` |
| `profiles: super_user select own` | `SELECT` | `(auth_user_id = auth.uid())` | — |
| `profiles: super_user update own` | `UPDATE` | `(auth_user_id = auth.uid())` | `(auth_user_id = auth.uid())` |
| `profiles: update own` | `UPDATE` | `(auth_user_id = auth.uid())` | `(auth_user_id = auth.uid())` |

### `regions`

| Policy name | Command | USING | WITH CHECK |
|---|---|---|---|
| `regions: authenticated select` | `SELECT` | `(auth.role() = 'authenticated'::text)` | — |
| `regions: select` | `SELECT` | `true` | — |
| `regions: service_role manage` | `ALL` | `(auth.role() = 'service_role'::text)` | — |
| `regions: super_admin delete` | `DELETE` | `(get_my_role() = 'super_admin'::app_role)` | — |
| `regions: super_admin insert` | `INSERT` | — | `(get_my_role() = 'super_admin'::app_role)` |
| `regions: super_admin update` | `UPDATE` | `(get_my_role() = 'super_admin'::app_role)` | `(get_my_role() = 'super_admin'::app_role)` |

### `trails`

| Policy name | Command | USING | WITH CHECK |
|---|---|---|---|
| `trails: admin delete local` | `DELETE` | `((get_my_role() = 'admin'::app_role) AND (region_id = get_my_region_id()))` | — |
| `trails: admin insert local` | `INSERT` | — | `((get_my_role() = 'admin'::app_role) AND (region_id = get_my_region_id()))` |
| `trails: admin select all` | `SELECT` | `(get_my_role() = 'admin'::app_role)` | — |
| `trails: admin update local` | `UPDATE` | `((get_my_role() = 'admin'::app_role) AND (region_id = get_my_region_id()))` | `((get_my_role() = 'admin'::app_role) AND (region_id = get_my_region_id()))` |
| `trails: authenticated select public` | `SELECT` | `((auth.role() = 'authenticated'::text) AND (visibility = 'public'::text))` | — |
| `trails: super_admin delete` | `DELETE` | `(get_my_role() = 'super_admin'::app_role)` | — |
| `trails: super_admin insert` | `INSERT` | — | `(get_my_role() = 'super_admin'::app_role)` |
| `trails: super_admin select all` | `SELECT` | `(get_my_role() = 'super_admin'::app_role)` | — |
| `trails: super_admin update` | `UPDATE` | `(get_my_role() = 'super_admin'::app_role)` | `(get_my_role() = 'super_admin'::app_role)` |
| `trails: super_user delete local` | `DELETE` | `((get_my_role() = 'super_user'::app_role) AND (region_id = get_my_region_id()))` | — |
| `trails: super_user insert local` | `INSERT` | — | `((get_my_role() = 'super_user'::app_role) AND (region_id = get_my_region_id()))` |
| `trails: super_user select all` | `SELECT` | `(get_my_role() = 'super_user'::app_role)` | — |
| `trails: super_user update local` | `UPDATE` | `((get_my_role() = 'super_user'::app_role) AND (region_id = get_my_region_id()))` | `((get_my_role() = 'super_user'::app_role) AND (region_id = get_my_region_id()))` |
| `trails: user select all` | `SELECT` | `(get_my_role() = 'user'::app_role)` | — |


